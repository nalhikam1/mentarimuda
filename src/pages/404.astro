---
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout title="404: Halaman Tidak Ditemukan">
    <div class="error-container">
        <div class="error-visual">
            <span class="error-code">404</span>
            <div class="sun-glow"></div>
        </div>
        
        <h1>Opps! Mentari Belum Terbit di Sini</h1>
        <p>Halaman yang kamu cari mungkin sudah berpindah, dihapus, atau memang tidak pernah ada.</p>
        
        <div class="action-buttons">
            <a href="/" class="btn-primary">Kembali ke Beranda</a>
            <button onclick="history.back()" class="btn-secondary">Kembali ke Sebelumnya</button>
        </div>
    </div>
</MainLayout>

<style>
    .error-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        min-height: 60vh;
        padding: 40px 20px;
    }

    .error-visual {
        position: relative;
        margin-bottom: 30px;
    }

    .error-code {
        font-size: 8rem;
        font-weight: 900;
        background: linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        line-height: 1;
        letter-spacing: -5px;
    }

    .sun-glow {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 150px;
        height: 150px;
        background: radial-gradient(circle, rgba(255, 142, 83, 0.2) 0%, transparent 70%);
        z-index: -1;
        filter: blur(20px);
        animation: pulse 4s infinite alternate;
    }

    h1 {
        font-size: 2rem;
        margin-bottom: 16px;
        color: var(--text);
    }

    p {
        color: var(--text-secondary);
        max-width: 480px;
        margin-bottom: 40px;
        line-height: 1.6;
    }

    .action-buttons {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
        justify-content: center;
    }

    .btn-primary {
        display: inline-flex;
        align-items: center;
        padding: 12px 24px;
        background: var(--text);
        color: var(--bg);
        text-decoration: none;
        border-radius: 12px;
        font-weight: 600;
        transition: transform 0.2s;
    }

    .btn-secondary {
        padding: 12px 24px;
        background: var(--card-bg);
        border: 1px solid var(--border);
        color: var(--text);
        border-radius: 12px;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.2s;
    }

    .btn-primary:hover { transform: translateY(-2px); }
    .btn-secondary:hover { background: var(--sidebar-bg); }

    @keyframes pulse {
        from { transform: translate(-50%, -50%) scale(1); opacity: 0.5; }
        to { transform: translate(-50%, -50%) scale(1.5); opacity: 0.8; }
    }

    @media (max-width: 640px) {
        .error-code { font-size: 5rem; }
        h1 { font-size: 1.5rem; }
    }
</style>
